version: '3.8'

services:
  exif-remover:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    container_name: exif-remover
    # Uncomment for development with live reload
    # volumes:
    #   - .:/app
    # command: go run .
